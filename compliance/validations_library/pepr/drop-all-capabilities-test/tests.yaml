pass:
  - test: default
    validation: validation.yaml
    resources: resources.json
    expected-validation: true
  - test: pod1-capabilities-not-dropped
    validation: validation.yaml
    resources: resources.json
    permutation: '.dropAllCapabilities |= map(if .metadata.name == "require-drop-all-capabilities-1" then del(.spec.containers[].securityContext.capabilities.drop) else . end)'
    expected-validation: false
