pass:
  - test: default
    validation: validation.yaml
    resources: resources.json
    expected-validation: true
  - test: pepr-uds-core-webhook-failure-policy-ignore
    validation: validation.yaml
    resources: resources.json
    permutation: '(.webhooks[] | select(.metadata.name == "pepr-uds-core") | .webhooks[] | select(.name == "pepr-uds-core.pepr.dev") | .failurePolicy) |= "Ignore"'
    expected-validation: false
